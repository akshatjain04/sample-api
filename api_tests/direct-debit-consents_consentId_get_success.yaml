# ********RoostGPT********

# Test generated by RoostGPT for test MiniProjects using AI Type Open AI and AI Model gpt-4-1106-preview
# 
# Test generated for /direct-debit-consents/{consentId}_get for http method type GET in artillery framework
# RoostTestHash=296226bc11
# 
# 

# ********RoostGPT********
config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 1
  payload:
    path: "direct-debit-consents_consentId_get_success.csv"
    fields:
      - "consentId"
      - "Authorization"
      - "x-fapi-auth-date"
      - "x-fapi-customer-ip-address"
      - "x-fapi-interaction-id"
      - "Accept-Language"
    skipHeader: true
    delimiter: "^|^"
  plugins:
    expect: {}

scenarios:
  - name: "Verify successful responses for direct-debit-consents API"
    flow:
      - log: "Starting the GET request for direct-debit-consents with a valid consentId"
      - get:
          url: "/direct-debit-consents/{{ consentId }}"
          headers:
            Authorization: "Bearer {{ Authorization }}"
            x-fapi-auth-date: "{{ x-fapi-auth-date }}"
            x-fapi-customer-ip-address: "{{ x-fapi-customer-ip-address }}"
            x-fapi-interaction-id: "{{ x-fapi-interaction-id }}"
            Accept-Language: "{{ Accept-Language }}"
          expect:
            - statusCode: 200
            - hasHeader: "x-fapi-interaction-id"
            - hasProperty: "data"
            - hasProperty: "data.consentId"
            - hasProperty: "data.creationDate"
            - hasProperty: "data.status"
            - hasProperty: "data.statusUpdateDate"
            - hasProperty: "data.permissions"
            - hasProperty: "links"
            - hasProperty: "links.self"
      - think: 1
