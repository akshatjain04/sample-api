# ********RoostGPT********

# Test generated by RoostGPT for test MiniProjects using AI Type Open AI and AI Model gpt-4-1106-preview
# 
# Test generated for /direct-debit-consents/{consentId}_get for http method type GET in artillery framework
# RoostTestHash=296226bc11
# 
# 

# ********RoostGPT********
config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 1
  payload:
    path: "direct-debit-consents_consentId_get_others.csv"
    skipHeader: true
    fields:
      - "consentId"
      - "Authorization"
      - "x-fapi-auth-date"
      - "x-fapi-customer-ip-address"
      - "x-fapi-interaction-id"
      - "Accept-Language"
  plugins:
    expect: {}

scenarios:
  - name: "Non-success API response testing"
    flow:
      # Test case: Invalid consentId
      - comment: "Sending a GET request with an invalid consentId"
      - get:
          url: "/direct-debit-consents/{{ consentId }}"
          headers:
            Authorization: "Bearer {{$processEnvironment.AUTH_TOKEN}}"
            x-fapi-auth-date: "{{ x-fapi-auth-date }}"
            x-fapi-customer-ip-address: "{{ x-fapi-customer-ip-address }}"
            x-fapi-interaction-id: "{{ x-fapi-interaction-id }}"
            Accept-Language: "{{ Accept-Language }}"
          expect:
            - statusCode: 404
            - hasHeader: "content-type"
            - headerEquals: {"content-type": "application/json"}
      - think: 1

      # Test case: Missing Authorization header
      - comment: "Sending a GET request without the Authorization header"
      - get:
          url: "/direct-debit-consents/{{ consentId }}"
          headers:
            x-fapi-auth-date: "{{ x-fapi-auth-date }}"
            x-fapi-customer-ip-address: "{{ x-fapi-customer-ip-address }}"
            x-fapi-interaction-id: "{{ x-fapi-interaction-id }}"
            Accept-Language: "{{ Accept-Language }}"
          expect:
            - statusCode: 401
            - hasHeader: "content-type"
            - headerEquals: {"content-type": "application/json"}
      - think: 1

      # Test case: Invalid Authorization token
      - comment: "Sending a GET request with an invalid Authorization token"
      - get:
          url: "/direct-debit-consents/{{ consentId }}"
          headers:
            Authorization: "Bearer invalid_token"
            x-fapi-auth-date: "{{ x-fapi-auth-date }}"
            x-fapi-customer-ip-address: "{{ x-fapi-customer-ip-address }}"
            x-fapi-interaction-id: "{{ x-fapi-interaction-id }}"
            Accept-Language: "{{ Accept-Language }}"
          expect:
            - statusCode: 403
            - hasHeader: "content-type"
            - headerEquals: {"content-type": "application/json"}
      - think: 1

      # Test case: Unsupported Accept-Language
      - comment: "Sending a GET request with an unsupported Accept-Language"
      - get:
          url: "/direct-debit-consents/{{ consentId }}"
          headers:
            Authorization: "Bearer {{$processEnvironment.AUTH_TOKEN}}"
            x-fapi-auth-date: "{{ x-fapi-auth-date }}"
            x-fapi-customer-ip-address: "{{ x-fapi-customer-ip-address }}"
            x-fapi-interaction-id: "{{ x-fapi-interaction-id }}"
            Accept-Language: "unsupported-language"
          expect:
            - statusCode: 406
            - hasHeader: "content-type"
            - headerEquals: {"content-type": "application/json"}
      - think: 1

      # Test case: Invalid URL
      - comment: "Sending a GET request to an invalid URL"
      - get:
          url: "/invalid-url/{{ consentId }}"
          headers:
            Authorization: "Bearer {{$processEnvironment.AUTH_TOKEN}}"
            x-fapi-auth-date: "{{ x-fapi-auth-date }}"
            x-fapi-customer-ip-address: "{{ x-fapi-customer-ip-address }}"
            x-fapi-interaction-id: "{{ x-fapi-interaction-id }}"
            Accept-Language: "{{ Accept-Language }}"
          expect:
            - statusCode: 404
            - hasHeader: "content-type"
            - headerEquals: {"content-type": "application/json"}
      - think: 1
