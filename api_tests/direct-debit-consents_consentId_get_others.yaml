# ********RoostGPT********

# Test generated by RoostGPT for test MiniProjects using AI Type Open AI and AI Model gpt-4-1106-preview
# 
# Test generated for /direct-debit-consents/{consentId}_get for http method type GET in artillery framework
# RoostTestHash=296226bc11
# 
# 

# ********RoostGPT********
config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 1
  payload:
    - path: "direct-debit-consents_consentId_get_others.csv"
      delimiter: "^|^"
      skipHeader: true
      fields:
        - "consentId"
        - "Authorization"
        - "x-fapi-auth-date"
        - "x-fapi-customer-ip-address"
        - "x-fapi-interaction-id"
        - "Accept-Language"
  plugins:
    expect: {}

scenarios:
  - name: "API Non-Success Responses Test"
    flow:
      # Request with invalid consentId
      - get:
          url: "/direct-debit-consents/{{ consentId }}"
          headers:
            Authorization: "Bearer {{ $processEnvironment.AUTH_TOKEN }}"
            x-fapi-auth-date: "{{ x-fapi-auth-date }}"
            x-fapi-customer-ip-address: "{{ x-fapi-customer-ip-address }}"
            x-fapi-interaction-id: "{{ x-fapi-interaction-id }}"
            Accept-Language: "{{ Accept-Language }}"
          expect:
            - statusCode: 404
            - hasHeader: "Content-Type"
            - headerEquals: { "Content-Type": "application/json" }
      - think: 1

      # Request with missing Authorization header
      - get:
          url: "/direct-debit-consents/{{ consentId }}"
          headers:
            x-fapi-auth-date: "{{ x-fapi-auth-date }}"
            x-fapi-customer-ip-address: "{{ x-fapi-customer-ip-address }}"
            x-fapi-interaction-id: "{{ x-fapi-interaction-id }}"
            Accept-Language: "{{ Accept-Language }}"
          expect:
            - statusCode: 401
            - hasHeader: "Content-Type"
            - headerEquals: { "Content-Type": "application/json" }
      - think: 1

      # Request with invalid Authorization token
      - get:
          url: "/direct-debit-consents/{{ consentId }}"
          headers:
            Authorization: "Bearer invalid_token"
            x-fapi-auth-date: "{{ x-fapi-auth-date }}"
            x-fapi-customer-ip-address: "{{ x-fapi-customer-ip-address }}"
            x-fapi-interaction-id: "{{ x-fapi-interaction-id }}"
            Accept-Language: "{{ Accept-Language }}"
          expect:
            - statusCode: 403
            - hasHeader: "Content-Type"
            - headerEquals: { "Content-Type": "application/json" }
      - think: 1

      # Request with unsupported Accept-Language header value
      - get:
          url: "/direct-debit-consents/{{ consentId }}"
          headers:
            Authorization: "Bearer {{ $processEnvironment.AUTH_TOKEN }}"
            x-fapi-auth-date: "{{ x-fapi-auth-date }}"
            x-fapi-customer-ip-address: "{{ x-fapi-customer-ip-address }}"
            x-fapi-interaction-id: "{{ x-fapi-interaction-id }}"
            Accept-Language: "unsupported_language"
          expect:
            - statusCode: 406
            - hasHeader: "Content-Type"
            - headerEquals: { "Content-Type": "application/json" }
      - think: 1

      # Request to an incorrect URL
      - get:
          url: "/incorrect-url/{{ consentId }}"
          headers:
            Authorization: "Bearer {{ $processEnvironment.AUTH_TOKEN }}"
            x-fapi-auth-date: "{{ x-fapi-auth-date }}"
            x-fapi-customer-ip-address: "{{ x-fapi-customer-ip-address }}"
            x-fapi-interaction-id: "{{ x-fapi-interaction-id }}"
            Accept-Language: "{{ Accept-Language }}"
          expect:
            - statusCode: 404
            - hasHeader: "Content-Type"
            - headerEquals: { "Content-Type": "application/json" }
      - think: 1
